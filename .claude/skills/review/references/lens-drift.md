---
purpose: review スキルの lens=drift（段階間ドリフト点検）の入力・観点・出力形式
note: 引数解釈・出力ディレクトリの解決・core.md の読込・連番・指摘3件の規律・サマリ形式は SKILL.md の共通骨格が担う。ここには drift 固有の部分だけを書く
---

# lens: drift（前段からのズレ）

AI が生成した中間成果物（`outline.md` / `draft.md`）が、前段の成果物から **主張を落としていないか**・**前段に根拠のない主張を足していないか** を点検する。掘って固めた思考が散文化・縮約される過程で静かにズレるのを、工程の切れ目で捕まえる。

ここで出た指摘は、著者が「この差分を許容するか／前段に戻して直すか」を決めるための材料。

## 入力

対象欄（`$2`）を **stage** として解釈する。`outline` か `draft` のどちらか。それ以外・空なら既定の `draft` とする。

遷移ごとに「健全な動き」が違うため、stage で基準と target を切り替える。

| stage | 基準（前段の正） | target（点検対象） | 主に見るドリフト |
|---|---|---|---|
| `draft`（既定） | `core.md`（核の正）＋ `thinking.md`（主張の正）＋ `outline.md`（構成の正）＋ `references.md`（根拠源） | `draft.md` | 落とした背骨／無根拠に湧いた主張 |
| `outline` | `core.md`（核の正）＋ `thinking.md`（主張の正） | `outline.md` | 縮約で落ちた背骨／構成段階で湧いた新主張 |

- **target**（必須）: 無い・空の場合は「target が見つからない（または空）ので点検していない。先に該当工程を回してください」という旨をサマリで返して終了する。**他ファイルで代替しない**
- **`thinking.md`**（必須）: 主張の正がなければドリフトを測れない。無ければサマリで欠落を報告して終了する
- `outline.md` / `references.md`: stage=`draft` で無ければ「無し」として進める

thinking.md 自体の生成（掘る工程）は点検しない。掘る工程は主張が増えるのが正常で、add を咎める点検は低価値なため。主張の是非を叩きたい場合は lens=critique を使う。

## 観点

基準に現れる**主張ユニット（idea）**と、target に現れる主張ユニットを、表現の長短を無視して **idea 単位**で対応付ける。1段落＝1主張とは限らない。対応付けた結果から、次の2軸だけを拾う。

**A. 落とした（基準にあり target に無い）**

- **背骨落ち ⚠**: core.md の「問い」「読後」を支える idea、主要な論点、主張の核を成す idea が target から消えている。最優先で報告する
- **意図的剪定（ok）**: 枝葉・重複・脇道で、落ちても主張は痩せない。健全な縮約。簡潔に列挙するに留める

判定基準は core.md の「問い」「読後」行と thinking.md のスコープ。それを支える idea が落ちていれば背骨落ち。

**B. 足した（target にあり基準に無い）**

- **無根拠 add ⚠**: 前段に根のない主張が target で湧いている。AI が著者の判断を経ずに中身を差し込んだ疑い。**どこにも根が無いことを明示**して最優先で報告する
- **根拠あり（grounded）**: references.md の資料や thinking.md の含意から自然に導ける肉付け・例示・接続。どの基準ファイルに根があるかを示す

散文化に伴う言い換え・具体例の補足それ自体は咎めない。**前段に無い新しい主張・評価・断定**が入っているかを見る。

## 出力テンプレート

見出しは `ドリフト点検`。`## 対象` 節には「{基準} → {target}（stage: {draft|outline}）」を書く。

lens 固有の追加節として、`## 指摘` の後ろに次を置く:

```markdown
## 健全な差分（参考・簡潔に）

- 意図的剪定: {落ちても主張が痩せない idea を短く列挙}
- 根拠ある肉付け: {湧いたが基準に根がある add を短く列挙、根の在処つき}
```

⚠ は共通骨格の `## 指摘` 節に優先度順で置く。各指摘の項目:

```markdown
### 1. {ドリフトした主張の見出し}
- 効く core 行: {問い / 読者 / 読後 / 動機 / 現在地 のどれに、どう効くか}
- 種別: {落とした背骨 ⚠ / 無根拠 add ⚠}
- 箇所: {落とした場合は基準のどこにあったか。足した場合は target の行番号}
- 問題: {落とした場合は target で消えている理由の推測。足した場合は前段のどこにも根が無いことの確認}
- 方向性: {戻すべきか剪定でよいか。残すなら根拠を補うか、著者判断を仰ぐべき点}
```

⚠ が1件も無い場合は `## 指摘` に「要確認なし（前段から逸脱していない）」と明記する。

## やらないこと

- 主張の正しさの判定（lens=critique の領分）。あくまで「前段から保たれたか／前段に根があるか」だけを見る
- 定量指標（carryover 率・件数比など）の算出。著者が知りたいのは *どの* idea がドリフトしたかであって率ではない
- 著者が書いた原稿（`draft_user.md`）の点検（lens=author の領分）。この lens は **AI が生成した** outline.md / draft.md が対象
- 著者の公開版との比較（`/compare-drafts` の領分）
