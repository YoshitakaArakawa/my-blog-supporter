# ドリフト点検 #02（20260621）

## 点検した遷移
thinking.md（主張の正）＋ outline.md（構成の正）＋ references.md（根拠源） → draft.md（stage: draft）

## 基準のコアメッセージ
VDS は歓迎すべき機能だが、有効化がサーバー単位・default on で「Viewer すらワークブックを迂回してデータソースを直接クエリしうる」統制モデルの変化が AI-readiness にグラデーションのある組織へ一斉に及ぶ。サイト単位の有効化制御を足し、組織が安全な速度で採用できるようにすべき。

## 前回からの変化
drift_01 は thinking/brief → outline の点検で、⚠ ゼロ・確認1点（CTA の射程を outline が「担当者ルート＋集合可視化」両採用で決着させた点）だった。今回の draft では結び CTA がまさにその両採用（担当者ルート＋記事コメント/SNS で可視化）で書かれており、前回の確認点は draft でも同じ決着で一貫している（新たなドリフトは生んでいない）。

## ⚠ 要確認（優先）

### 落とした背骨
- **同一 PDS 共用の問い直し（人間中心設計の PDS をエージェント直クエリ面に流用してよいかという利用モデルの問い直し）** — thinking.md 観点3 で「概念2点に格上げ」された核の片方（観点3.1）。outline でも章4「AI 用に統制済みデータソースへ移行する時間軸の余裕」に接続される前提だった。draft では、この「設計思想としての問い直し」が概念として展開されておらず、章4 の「AI 用に統制済みのデータソースを用意していく移行」「データソース側に列の制御や行レベルセキュリティを移せばいい…の正論」という *移行運用の一句* と、冒頭の著者メモ（公開しない）の「専用のデータソースを AI 用に分ければ」に縮約・退避されている。/ 消えた理由の推測: critique_01 指摘5 が「専用 PDS を分ける＝資産単位の正攻法／移行期間を site トグルで稼ぐ」と運用フレームに寄せたため、設計思想の問いが運用論に吸収された。/ 方向性: コアメッセージ（二層・アハ・グラデーション・提案）の達成には必須ではなく、「移行を安全に走らせる時間」という形では背骨に残っている。ただし thinking が概念として格上げした論点なので、設計思想の問いとして一文でも本文に戻すか、運用論への縮約を著者が許容するかは確認したい。背骨落ちか健全な剪定かの境界線上の1件。

### 無根拠に足した主張
- 要確認なし（target に湧いた、前段に根の無い主張は検出されなかった）。permission flag 引用・Server/Cloud の制御粒度・fail-open・Prep Conductor 同型・採用ペース着地など draft の各 idea は thinking.md／outline.md／references.md のいずれかに根がある。系譜章の intent（開発者向け設計）も「ここは自分の想像が混じる」「推測」とラベルされており、thinking 観点5 の推測ラベル方針どおり。

## 健全な差分（参考・簡潔に）

- 意図的剪定（落ちても主張が痩せない）:
  - 観点3 補足の RLS 実装位置（ワークブック側実装だと VDS 直クエリで効かない）は draft 章2「ワークブックで隠していた列／所属部署で絞っていた表示はそもそも通りません」に吸収され、独立箇条にはしていない。thinking でも「触媒として軽く」位置づけで健全。
  - thinking「別記事候補・書かない論点」（サービスアカウント×RLS／embedded／監査ログ／FDQ）はスコープ外宣言どおり draft 本文に持ち込んでいない。正しい剪定。
  - 著者向けの想定反論の控え（擬似 site deny／専用 PDS 分離／刃の出典の性質／Server-Cloud 事実精度）は本文ではなく冒頭の「公開しない著者メモ」に退避。防御文書化を避ける voice-style に沿った処理で、背骨落ちではない。
- 根拠ある肉付け（target で具体化したが基準に根あり）:
  - 人事ダッシュボードの具体例（集計に使うが表示しない列／所属部署フィルター／ユーザー関数の出し分け）— outline 章2 素材・references.md §背景に根。新主張ではなく既存 idea の具体化。
  - 「Headless な分析体験」「素直にわくわくします」等の礼賛の語り口 — thinking「機能も世界観も広げたい」＋brief 礼賛トーンに根。
  - 章3 の「数百のサイト・数千のプロジェクト／新規が日々増える」描写 — critique_01 指摘3（fail-open）＋thinking 観点4 に根。

## 総評
draft は thinking / outline / references の背骨を高い忠実度で散文化しており、無根拠 add はゼロ。著者が確認すべきは1点のみ:「同一 PDS 共用の問い直し」（thinking が概念2点の片方に格上げした論点）が、draft では設計思想の問いとしてではなく「移行を安全に走らせる時間」という運用句へ縮約され、より明示的な議論は公開しない著者メモへ退避している。これを許容できる剪定とみるか（コアメッセージ達成には支障なし）、設計思想の問いを本文に一文戻すかは著者判断。この1点を裁定すれば draft は次工程へ進めてよい。
