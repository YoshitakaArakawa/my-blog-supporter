# textlint-filter-rule-allowlist の許可リスト。
# ここにマッチする範囲は textlint の全ルールから除外される。
# 著者向けメモ（ℹ️ / ✍️ の引用ブロック）、HTML コメント、URL は本文ではないので対象外にする。
- "/^>.*(ℹ️|✍️).*$/m"
- "/<!--[\\s\\S]*?-->/"
- "/https?:\\/\\/[^\\s)]+/"
# 著者の署名文（ルール9 のサインオフ）。感嘆符ルールを切っているので通常は不要だが念のため
- "それでは！"
